# Flutter client configuration.
# These are not Gemini secrets.
#
# Use these values as --dart-define entries when running the Flutter app.
# Keep this file as placeholders only. Put real local values in an uncommitted
# .env file or pass them directly as --dart-define values.

ACCESSPULSE_AI_FUNCTION_URL=https://YOUR_SUPABASE_PROJECT_REF.supabase.co/functions/v1/analyze-evidence
ACCESSPULSE_SUPABASE_ANON_KEY=PASTE_YOUR_SUPABASE_ANON_KEY_HERE
